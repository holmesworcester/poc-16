"""Measure pile/closure economics for docs/COSTS.md.

STUB: the previous harness drove tree.build FAT, which died with the fact
tree in the oyd.5 demolition sweep. poc-16-oyd.7 rewrites this measurement
over the manifest spine (home-leaf piles + closure siblings) so COSTS.md
numbers can be re-derived against the one-store layout.
"""
import sys

if __name__ == "__main__":
    sys.exit(
        "bench/measure_piles.py: harness removed with the fact tree; "
        "poc-16-oyd.7 rewrites it over the manifest spine.")
